/*! marked 4.3.0 | MIT License | https://github.com/markedjs/marked */
(function(global,factory){typeof exports==="object"&&typeof module!=="undefined"?module.exports=factory():typeof define==="function"&&define.amd?define(factory):(global.marked=factory());})(this,function(){function marked(src){return marked.parse(src);}marked.parse=function(src){
  // Soporte para títulos (###, ##, #)
  var html = src
    .replace(/^### (.*$)/gim, '<h3>$1</h3>')
    .replace(/^## (.*$)/gim, '<h2>$1</h2>')
    .replace(/^# (.*$)/gim, '<h1>$1</h1>')
    // Negritas y cursivas
    .replace(/\*\*(.*?)\*\*/g, '<strong>$1</strong>')
    .replace(/\*(.*?)\*/g, '<em>$1</em>')
    // Listas con guion
    .replace(/^\- (.*$)/gim, '<li>$1</li>')
    // Listas numeradas
    .replace(/^\d+\. (.*$)/gim, '<li>$1</li>')
    // Bloques de código multilinea
    .replace(/```([\s\S]*?)```/g, '<pre><code>$1</code></pre>')
    // Bloques de código inline
    .replace(/`([^`]+)`/g, '<code>$1</code>')
    // Saltos de línea dobles por párrafos
    .replace(/\n\n/g, '<br><br>')
    // Saltos de línea simples
    .replace(/\n/g, '<br>');
  // Agrupa <li> en <ul>
  html = html.replace(/(<li>.*?<\/li>)/gs, function(match){
    return '<ul>' + match.replace(/<br>/g,'') + '</ul>';
  });
  return html;
};return marked;});

